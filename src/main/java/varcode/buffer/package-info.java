/*
 * Translates between an in memory Object representation to a 
 * text/code representation.  
 * <PRE>float f = 10.234 -> "10.234f"
 * String[]{"A","B","C"} -> "A, B, C"
 * Map.class -> "java.util.Map"
 * Character c = 'g';  -> "'g'"
 * </PRE>
 */
package varcode.buffer;
