/**
 * Authoring is the act of specializing a {@code Template} with data (vars) and 
 * functionality (@link VarScript}s, 
 * {@link varcode.author.PreProcessor}s, 
 * {@link PostProcessor}s
 * into a textual form.
 */
package varcode.author;
